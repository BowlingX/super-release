# super-release releases itself

branches:
  - main
  - master

tag_format: "v{version}"

plugins:
  - name: changelog
  - name: npm
  - name: git-commit
    options:
      message: "chore(release): {releases} [skip ci]"
  - name: git-tag
